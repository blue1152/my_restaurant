<!-- ./views/edit.handlebars -->
<form action="/restaurants/{{{restaurants.id}}}/edit" method="POST">
  <input type="text" placeholder="name" name="name" value="{{{restaurants.name}}}">
  <button type="submit">Edit</button>
</form>